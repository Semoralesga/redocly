get:
  tags:
    - Items
  summary: Encontrar las unidades de los ítems
  description: Retorna una lista de unidades de los ítems
  operationId: fetchItemUnits
  responses:
    '200':
      description: Operación exitosa
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  allOf:
                    - $ref: ../components/schemas/JSONAPIResourceObject.yaml
                  properties:
                    attributes:
                      type: object
                      properties:
                        description:
                          type: string
    default:
      description: |
        **404**: No se encontraron unidades para los ítems\
        **500**: Error en el servidor
      content:
        application/json:
          schema:
            $ref: ../components/schemas/JSONAPIErrorResponse.yaml
