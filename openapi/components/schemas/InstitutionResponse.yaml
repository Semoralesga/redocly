type: object
allOf:
  - $ref: ./JSONAPIResourceObject.yaml
  - type: object
    properties:
      attributes:
        type: object
        properties:
          address:
            type: string
            description: Dirección física
          cityId:
            type: string
            description: >-
              `id` de la ciudad (obtenido a través del API). Sólo se permite una
              oficina por ciudad
          headquarter:
            type: boolean
            description: >-
              Sede principal: `true` si es la sede principal, `false` si es una
              sucursal
          idNumber:
            type: string
            description: Número de identificación oficial
          idType:
            type: integer
            description: '`id` del tipo de identificación (Obtenida a través del API)'
          name:
            type: string
            description: Razón social
          mnemonic:
            type: string
            description: Nemotécnico de la institución
